# Copy to .env (gitignored) and fill in your Neon connection string.
# Locally this file is loaded automatically at import time; variables already
# present in the real environment always take precedence.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"
